def driven_cost(Total):
	return (float(d_miles) / m_per_gallon) * doll_per_gallon

d_miles = float(input())
m_per_gallon = float(input())
doll_per_gallon = float(input())
Total = '{:.2f}'.format(round((float(d_miles) / m_per_gallon) * doll_per_gallon,2))
 
print(Total)

d_miles = float(input())
m_per_gallon = float(input())
doll_per_gallon = float(input())
Total = '{:.2f}'.format(round((float(d_miles) / m_per_gallon) * doll_per_gallon,2))

print(Total)
d_miles = float(input())
m_per_gallon = float(input())
doll_per_gallon = float(input())
Total = '{:.2f}'.format(round((float(d_miles) / m_per_gallon) * doll_per_gallon,2))

print(Total)
